let {defaultLayoutData} = require('./layout/layout_data');
console.log(defaultLayoutData);

/*var blessed = require('blessed')
, screen = blessed.screen();

let GlobalMngDlg = require('./tools/helper/global_mng_dlg.js')
var dlg = new GlobalMngDlg(1);
screen.append(dlg.ui);

screen.key(['escape', 'q', 'C-c'], function(ch, key) {
    screen.destroy();
    return process.exit(0);
  });
  screen.render();
*/